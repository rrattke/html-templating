{
  "name": "html-templating-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@vanishing/framework && npm run build --workspace=example-app",
    "dev": "npm run dev --workspace=example-app",
    "start": "npm run dev --workspace=example-app"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

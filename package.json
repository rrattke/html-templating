{
  "name": "html-templating-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=example-app",
    "start": "npm run dev --workspace=example-app"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
